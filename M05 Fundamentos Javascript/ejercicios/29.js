function obtenerAreaRectangulo(alto, ancho) {
  // Retornar el área de un rectángulo teniendo su alto y ancho.
  // Tu código:
  //formula Area= alto*ancho
  var area= alto * ancho;
   return area;
}
//console.log(obtenerAreaRectangulo(4,10));
module.exports = obtenerAreaRectangulo;
