function mayorQueCincuenta(num) {
  // Retorna true si el argumento "num" es mayor que cincuenta.
  // De lo contrario, retorna false.
  // Tu código:
  // si el numero ingresado por el usuario es mayor a 50 dara true, de contrario dara false
  if(num > 50){
    return true;
  }else{
    return false;
  }
}
//Probamos el codigo
//console.log(mayorQueCincuenta(70));
module.exports = mayorQueCincuenta;
